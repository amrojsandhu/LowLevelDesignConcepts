package oops.assignments.feature.gating.brackets;

import oops.assignments.feature.gating.Token;

public abstract class SpecialChars extends Token<Character> {

    public SpecialChars(Character token) {
        super(token);
    }
}
